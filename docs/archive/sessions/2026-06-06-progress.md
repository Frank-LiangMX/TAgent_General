# 2026-06-06 工作进度（给明早新会话用）

> **生成时间**：2026-06-06 22:50
> **覆盖范围**：今天 12:00 - 22:50（~11 小时）
> **产出**：26 个 git commit，10 个设计章节，93 个单元测试

---

## TL;DR

今天做了 **3 件大事 + 1 件收尾**：

1. **Tier 1 + T2 品牌清理**（10 commit）—— 把所有 "proma" 标识清干净
2. **6 个设计章节拍板**（6 commit, 决策 #7-#12）—— TA 模式 / 缓存 / 命名 / 记忆 / 工作区 / context 管理
3. **§8.4 context 管理 6/7 项落地**（9 commit, P0-P2 实现）—— 修了 9120caac 那类 400 (2013) 错的根因
4. **收尾**（这文件 + 接续摘要 UPDATE 段）

**剩 1 项**：P1-3 客户端 `compact_session` 工具（最大件, 2-3 天, 下一会话做最小版）

---

## 1. Tier 1 品牌清理（5 commit）

按用户原话"全清 proma 标识"，每个一 commit 方便回滚：

| Commit | 改动 | 文件数 |
|---|---|---|
| `8e5e9ab` | `proma-file://` → `tagent-file://` (OS 协议) | 6 |
| `05c525c` | `~/.proma-dev/` → `~/.tagent-dev/` | 4 |
| `d6c4e32` | `appId: com.proma.app` → `com.tagent.app` | 1 |
| `935064e` | `proma-share` / `proma-backup` → `tagent-*` 扩展名 | 8 |
| `7018bba` | `proma-coach` → `tagent-coach` (默认 skill) | 1 (+目录 rename) |

## 2. T2 残留清理（5 commit）

发现 T2 实际比 Tier 1 涉及面更大（9-10 commit 才对，summary 估 1 file 严重偏低）：

| Commit | 改动 | 文件数 |
|---|---|---|
| `9b81d9e` | in-app state / DOM / 临时目录 / URL (T2.1+T2.2) | 51 |
| `7c56f97` | 品牌 Logo 资源文件 (15 个 git mv) | 19 |
| `8baaee2` | yml + package.json 描述 | 5 |
| `e9d05d9` | 生产配置目录 `~/.proma/` → `~/.tagent/` (T2.4B) | 45 |

## 3. 设计文档补完（6 commit, 决策 #7-#12）

文档 955 行 → **1803 行**（+848 行）。

| Commit | § | 决策 |
|---|---|---|
| `9c144cd` | §3.3 + #7 | TA 模式数据布局（`~/.tagent/ta/` 统一根）|
| `f3546c3` | §3.4 + #8 | 缓存机制 + 目录规范（A1+B2+C1 组合）|
| `eea2bad` | §3.4 L→C + #9 | 缓存层 C1-C5 命名（解决与 §6 记忆层 L0-L5 冲突）|
| `97f3f31` | §6.5 + #10 | 记忆自进化（L0 双视图 + 4 机制 + 8 生命周期）|
| `5298230` | §3.5 + #11 | 工作区 UI 重构（D 方案 v2：左侧 2 列并排）|
| `6c45cd9` | §8.4 + #12 | Context 管理机制（7 项改进方案）|

## 4. §8.4 Context 管理 6/7 实现（9 commit）

排查 9120caac 6.8MB 报错后，开始真修：

| Commit | 优先级 | 改动 | 工时 |
|---|---|---|---|
| `f98ab7f` | 🔴 P0-2 | `validateChannelModel()` 函数 + 3 helpers (anthropic/openai/google) | 1h |
| `eb17638` | 🔴 P0-2 | 启动时 `validateAllEnabledChannels()` | 30min |
| `8e0f09c` | 🔴 P0-1 | 动态 token 预算（替代硬编码 `MAX_CONTEXT_MESSAGES = 20`）| 1h |
| `e86e706` | 🟡 P1-1 | tool summary 按 token 截断 + 头尾保留 | 30min |
| `031c276` | 🟡 P1-2 | 图片 placeholder 注入 (`[本消息含 N 张图片]`) | 30min |
| `f7ecac0` | 🔴 P0-2 UI | ChannelSettings 加 "测试 model" 按钮 + preload wrapper | 1h |
| `d6e0ed2` | 🟢 P2-2 | 圆环 3 态颜色（70 黄 / 90 红）| 30min |
| `a3a0b87` | 🟢 refactor | 提取 `agent-context-utils.ts` + **22 个单测**（71→93）| 1.5h |
| `46e121f` | 🟢 P2-1 | Nudges 80% / 90% toast 提示 | 30min |

## 5. 关键设计决策（容易忘的）

### 命名空间
- **L0-L5**：记忆层（与 hermes Honcho / GenericAgent 兼容，§6 全部保留 L 命名）
- **C1-C5**：缓存层（§3.4 改名，避免与 L 冲突）

### TA 模式路径
- `~/.tagent/ta/`（`tag_store/`, `configs/`, `memory/`, `ue5_bridge/`, `sessions/`, `usage_log.jsonl`, `pipeline_runs.jsonl`）
- ta_agent 启动用 `TA_AGENT_DATA_DIR` env 指向
- 通用模式数据继续在 `~/.tagent/` 根下

### 缓存 4 类
- **C1 持久** / **C2 缓存** (7 天 LRU) / **C3 日志** (50MB 滚动) / **C4 临时** (1-3 天) / **C5 Electron 内部**

### Electron userData 合并
- `apps/electron/src/main/index.ts` 用 `app.setPath('userData', ...)` 指向 `~/.tagent[-dev]/electron-userdata/`
- 替代默认 `%APPDATA%/@tagent/electron-dev/` 和 `%APPDATA%/TAgent/`

### 5 概念工作区
- ① 工作区（抽象容器）
- ② workspace-files/（Agent 内部笔记）
- ③ 附加工作区目录（外部代码根，Claude Code cwd 等价物）
- ④' 附加工作区文件（外部单文件）
- ⑤ 会话附加目录（仅当前 session）

### D 方案 v2 UI
- 左侧 2 列并排：60% 工作区管理 + 40% 会话列表
- 中部 Chat/Agent
- 右侧 2 Tab：会话文件 + 文件改动（**删**除"工作区文件" Tab）

### Context 管理 6 项已实现
1. ✅ P0-1 动态 token 预算（接 `contextWindow`）
2. ✅ P0-2 model 验证（启动 hook + UI 按钮 + IPC）
3. ✅ P1-1 tool summary 按 token 截断
4. ✅ P1-2 图片 placeholder
5. ✅ P2-1 Nudges 80%/90%
6. ✅ P2-2 圆环 3 态颜色

### 剩 1 项（下一会话做）
- ❌ **P1-3 客户端 `compact_session` 工具**（最大件）
  - SDK 服务端压缩失败时的 fallback
  - 3 策略：`summarize` / `drop_old_tool_results` / `keep_last_n`
  - 需要：LLM tool 定义 + IPC handler + UI 集成 + 实际压缩逻辑
  - 建议最小版：先做 `drop_old_tool_results` 策略（最简单，价值最高）

## 6. 关键代码位置（明早找代码用）

| 内容 | 位置 |
|---|---|
| model 验证函数 | `apps/electron/src/main/lib/channel-manager.ts` (`validateChannelModel` + 3 helpers) |
| 启动 hook | `apps/electron/src/main/lib/runtime-init.ts` (`validateAllEnabledChannels`) |
| 动态 token 预算 | `apps/electron/src/main/lib/agent-context-utils.ts` (`computeMaxContextMessages`) + `agent-orchestrator.ts:buildContextPrompt` |
| Tool summary 截断 | `apps/electron/src/main/lib/agent-context-utils.ts` (`summarizeToolResult`) |
| 图片 placeholder | `apps/electron/src/main/lib/agent-orchestrator.ts:buildContextPrompt` |
| 测试 model 按钮 | `apps/electron/src/renderer/components/settings/ChannelForm.tsx` |
| 圆环 3 态 | `apps/electron/src/renderer/components/agent/ContextUsageBadge.tsx` |
| Nudges 80/90 | `apps/electron/src/renderer/components/agent/ContextUsageBadge.tsx`（useEffect）|
| 单测 | `apps/electron/src/main/lib/agent-context-utils.test.ts` (22 个) |
| 设计文档 | `docs/plans/2026-06-05-tagent-fusion-design.md` |

## 7. 用户协作风格（备忘）

- Frank Danny, 喜欢**直接给状态** + **结构化表格** + **commit hash 报告**
- 中英文混用，偏好中文，**技术术语保留英文**
- **凌晨 1 点也还在干**，但 agent 应该在关键决策点主动提示休息
- 接受"先做主流程 / 收尾小件可后做"的工作模式
- 喜欢按"agent 提议 → 用户拍板"流程

## 8. 用户已拍板且不可逆的决策

| 决策 | 内容 | 不要做的事 |
|---|---|---|
| 品牌策略 | 全清 "proma" 标识 | ❌ 不要回退 |
| 数据丢失 | 接受 | ❌ 不要为兼容性做迁移脚本 |
| 上游追踪 | 不需要 Proma 仓库 | ❌ 不要加 git remote |
| 目录名 | `F:\TAgent_General\`（无下划线）| ❌ 不要改 `F:\TAgent` |
| TA 模式数据根 | `~/.tagent/ta/` | ❌ 不要回退到 `~/.ta_agent/` |
| 命名空间 | 记忆 L / 缓存 C | ❌ 不要全统一 |
| context 验证 | 启动时 + UI 双触发 | ❌ 不要只留一个 |
| 工作区 UI | D 方案 v2（左侧 2 列）| ❌ 不要改 tab 切换 |

## 9. 不要碰的清单

- ❌ `proma-thinking/` 目录
- ❌ 默认 skill 文档里 `~/.proma/...` 路径引用
- ❌ `download-bun.ts:235` 的 `pkg.proma?.bun?.version` legacy 字段
- ❌ README.md / CLAUDE.md / release-notes（Tier 3 用户文档）
- ❌ F:\Proma 拉新东西（legacy 仓库）
- ❌ Tier 2 注释里 125+ 处 "Proma" 引用

## 10. 明天第一句话怎么写

新会话开始时，把这段贴进去：

```
我之前在做 TAgent 项目（迁移自 Proma 框架 + 集成 ta_agent 领域能力）。
昨天干了 26 个 commit 完成 Tier 1+2 品牌清理 + 6 个设计补完 + §8.4 context 管理 6/7 项。

📋 摘要：F:\TAgent_General\.context\2026-06-06-progress.md
📋 设计：F:\TAgent_General\docs\plans\2026-06-05-tagent-fusion-design.md（§8.4 context 管理）

当前任务：继续做 §8.4 剩 1 项 — P1-3 客户端 `compact_session` 工具
  最小版：先做 `drop_old_tool_results` 策略（半天工）
  - 新 LLM tool 定义
  - IPC handler + preload wrapper
  - 实际压缩逻辑：扫描 session.jsonl，丢老 tool_use/tool_result 对，保留文本
  - UI 集成：ContextUsageBadge popover 加"客户端压缩"按钮
  - 测试

先读完上面两份文档，然后告诉我"开始 P1-3 最小版"或者问任何不清楚的。
```

## 11. 可能要做但还没做的事（优先级排）

1. 🟡 P1-3 最小版（半天, 最大价值）—— SDK 压缩失败的 fallback
2. 🟡 P1-3 完整版（2-3 天）—— 3 策略
3. 🟢 起 dev 验证 9 个新功能（30min）—— 实际跑 toast/按钮/启动日志
4. 🟢 chat-service 集成测试（半天）—— 防回归
5. ⚪ 接续摘要里 link 这个 progress note（已做了）
6. ⚪ 测试覆盖 `validateChannelModel`（P0-2）—— 需要 mock fetch，工作量大
7. ⚪ 写 P1-1 / P1-2 单测 —— 容易补，1-2h

## 12. 我自己需要警惕的事

- 别再 "P0 拍板 8 个" 误报 — 12 个决策就是 12 个
- 别把 L vs C 搞混 — 记忆 L / 缓存 C
- 写代码时**先验证（typecheck + test）再 commit**
- 用户不接受"未完成"代码 commit —— 每个 commit 都应该跑得动
- 引用 §8.4 时要说清是"§8.4 决策 #12"，不是"§8.4 P0-2"
