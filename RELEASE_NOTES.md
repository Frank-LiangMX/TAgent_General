# v1.7.0

Spatial Shell 空间材质重构 + 动画虚拟角色 + 看板目标模式 + CSV 分析工具。

## ✨ 新功能
- **Spatial Shell 空间壳层** — 全新材质系统，左侧 rail 改为 Soft UI 独立浮岛，右侧 inspector 支持 morph 动画与焦点模式；会话状态栏移除，班组进度合并到右栏 crew 按钮；记忆页面改为 rail-only 单列布局
- **动画虚拟角色** — assistant presence 软体动画，会话时有呼吸、眨眼、软体晃动等拟人交互
- **看板目标模式** — `goal_mode` 支持验收标准与 worker 白名单，任务完成自动闸门判定
- **CSV 分析工具** — `csv_prepare` + `csv_query` + `csv_dashboard` 三件套，支持 100 万+ 行流式处理
- **Skill Curator 自进化** — 技能调用埋点与徽章，自动沉淀高频使用技能
- **运行时消息队列** — Agent 消息队列管理，优化流式渲染性能
- **Windows Portable 免安装包** — 单文件即用，无需安装

## ⚡ 性能优化
- 长会话切换分批挂载 — 50+ turn 会话秒切，消除切换动画卡顿
- 流式时旧思考块自动折叠 — 消除长任务卡顿闪烁
- CSV 大文件流式优化 — 100 万+ 行不卡顿

## 🐛 修复
- worker 详情弹窗固定高度 — 运行中不再大窗小窗闪
- 预览刷新版本号收窄到文件级 — 改 A 文件不再牵连同会话 B 预览
- 侧栏会话选中改主题实色 — 顺滑过渡
- Context 占用面板重做 — kscc 渠道隐藏失真圆环
- 会话标题完整落盘 — 侧栏截断与主题 Tooltip 对齐标签页
- 自动更新机制修复 — 旧版本可无感更新
- 滚动时禁用 backdrop-filter — 消除拖影
- 滚动按钮点击跑位修复 — 完成铭牌扁平化
- 代码高亮匹配主题 — 深色/浅色模式切换后高亮同步
- 孤儿引用修复 — 会话删除时标记记忆层已删除条目
- Welcome 页面延迟显示 — 等标签状态水合后再显示
- 浏览器 sandbox 移除 — 允许 file:// URL 加载
- 项目创建按钮可见性修复
- 会话刷新时保留实时 turn — 不再丢失流式内容
- 批量删除模式下会话项标题右对齐修复
- 侧栏会话列表布局优化
- macOS 窗口按钮与 Soft UI rail pill 位置修复

## 🎨 样式
- 统一圆角 8px → 12px
- 非默认主题左上角团光强度压低
- 输入框控制栏选择态与权限模式视觉优化
- 用户气泡软阴影 + 右侧 inspector morph 动画
- 设计画布改为 Spatial Lens 原型
- 侧栏 chrome、深色表面、设置壳层打磨
- 输入框底衬、模型选择器、玻璃弹窗优化
- 场景漫射三色、气泡软阴影、方向边缘光
- 对话 chrome 与侧栏工具集群对齐
- Soft UI 圆形左侧 rail 激活态
- 顶部安全区融合浮动岛，去除空 band

---

# v1.6.1

看板数字员工体验 + Agent 输入区稳定性 + 流式/统计补丁。

## ✨ 新功能
- 看板默认角色补齐、角色工号与 worker Skills 挂载
- 数字员工轻量拟人化 — 点将话术 + 跨材质工牌卡（人态文案）
- 右栏班组墙 — 伴生面板看员工队列与任务摘要
- 角色统计体系 — 上岗/工时/日周月窗口 + 角色档案弹窗
- 流式输出设置（可开关）
- 单次 Agent 通话统计展示

## 🐛 修复
- 移除 `getContextUsage` 调用链，修复 GLM 等渠道异常高频调用
- 运行中计时胶囊与会话内容左对齐（执行过程 shell 展开时不再偏移）
- 任务进度条仅在当前流式回合有进行中任务时显示，结束后立即隐藏
- 输入框不再常驻附加目录 chip（关闭即卸载干扰输入）
- 打包后任务栏图标跟随主题

## 🧹 清理
- SessionTeamTab 降级，班组主路径改到右栏
- release 脚本增加 preflight（版本号 / notes / typecheck / eslint）
